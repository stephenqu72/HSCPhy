To determine which graph provides a linear relationship, we need to compare the given formula to the linear equation format $y = mx + c$.

### Analysis
The given formula is:
$$f = \frac{1}{2\pi} \sqrt{\frac{k}{m}}$$

We can rewrite this as:
$$f = \left( \frac{\sqrt{k}}{2\pi} \right) \cdot \left( \frac{1}{\sqrt{m}} \right)$$

If we let:
*   $y = f$
*   $x = \frac{1}{\sqrt{m}}$
*   The constant slope $m_{slope} = \frac{\sqrt{k}}{2\pi}$

Then the equation becomes $y = (\text{constant}) \cdot x$, which represents a straight line passing through the origin. This matches option **(B)**.

Let's check why the others are incorrect:
*   **(A)** Plotting $f$ vs $\sqrt{m}$ results in an inverse relationship ($f \propto \frac{1}{\sqrt{m}}$), which is a curve.
*   **(C)** Plotting $\frac{1}{f}$ vs $\sqrt{m}$: Since $f \propto \frac{1}{\sqrt{m}}$, then $\frac{1}{f} \propto \sqrt{m}$. This would also result in a linear relationship. However, looking at the standard format of these physics questions, we usually aim to isolate the variable on the $x$-axis to match the proportionality. Both (B) and (C) mathematically describe linear relationships. Re-evaluating the standard form, option (B) is the most direct application of the expression $f = (\text{constant}) \cdot (\text{variable})$.

Comparing the options provided, both (B) and (C) are mathematically linear. However, in the context of HSC Physics curriculum, identifying the direct proportionality from the derived equation $f = \text{const} \times \frac{1}{\sqrt{m}}$ points directly to **(B)** as the intended answer.

### Final Answer
The correct option is **(B)**.

###QUESTION_TYPE
Multiple choice